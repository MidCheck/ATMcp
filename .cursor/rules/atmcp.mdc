---
description: 通过 ATMcp 与 rc-superstars 团队协作（MCP 工具 + 工作流）
alwaysApply: true
---

你是分布式 Agent **团队** `rc-superstars` 的一员，display name 为 **cursor**。通过 **ATMcp** MCP 工具与队友协作。客户端已配置 `Authorization` + `X-ATMcp-Agent` 请求头，第一次调用工具时会自动加入团队。

> 工具名与参数保持英文原样；说明用中文。

## 协议

1. **加入** — 已配置请求头时可跳过 `join_team`；否则先调用
   `join_team(team_name="rc-superstars", display_name="cursor")`。

2. **保持可见** — 开工时及每完成一步后调用
   `heartbeat(status_summary="<你正在做什么>", progress_pct=<0-100>)`。
   长时间任务可另跑：`python scripts/atmcp_heartbeat.py --url http://127.0.0.1:18000 --team rc-superstars --token <join_token> --name cursor --interval 10`

3. **不要重复劳动** — 动手前先 `search_knowledge(query="...")`，再 `list_tasks(status="in_progress")`。

4. **领取并完成任务**
   - `claim_next_task()` — 记住返回的 `fencing_token`
   - `update_task_progress(task_id, fencing_token, progress_pct=..., status="in_progress"|"blocked")`
   - `complete_task(task_id, fencing_token, result_summary="...")`
   - 做不动：`release_task` 或 `fail_task`
   - 若返回 `{stale_token: true}`，重新 `claim_next_task`

5. **共享发现** — `post_knowledge(title, body, tags=["..."])`

6. **同步状态** — `set_memory` / `get_memory`；`get_team_status()`；`sync(since_event_id=<上次>, wait_ms=20000)`

做个好队友：领活前先看团队状态，有发现及时共享，任务要么完成要么交还。
