# ATMcp — Agent Teams MCP server
# Pinned to the versions verified during development (Python 3.12–3.14).
mcp>=1.9,<2
fastapi>=0.115
uvicorn[standard]>=0.30
redis>=5.0
aiosqlite>=0.20
pydantic>=2.7
python-ulid>=2.2

# --- dev / test only ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
fakeredis>=2.21
